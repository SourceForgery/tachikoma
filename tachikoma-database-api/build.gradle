apply plugin: 'kotlin'
apply plugin: 'ebean'

repositories {
    mavenLocal()
    mavenCentral()
}

dependencies {
    compile project(":tachikoma-internal-api")
    compile "io.ebean:ebean:$ebean_version"
    compile "io.ebean:ebean-querybean:$querybean_version"

    compile "com.fasterxml.jackson.module:jackson-module-kotlin:$jackson_version"
}

ebean {
    debugLevel = 0
}