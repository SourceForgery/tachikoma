apply plugin: "base"

task assembleArtifact(type: Zip) {
    archiveName 'backend-api-grpc.zip'
    from "src/main/proto"
    description "Assemble archive $archiveName into ${relativePath(destinationDir)}"
}


assemble.dependsOn assembleArtifact
build.dependsOn assemble